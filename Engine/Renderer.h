#pragma once

#include <raylib.h>

namespace Engine_Core
{
    struct ApplicationSettings;

    class Renderer
    {
        public:
            Renderer();
            ~Renderer();

            void Render();

        private:
    };
}
